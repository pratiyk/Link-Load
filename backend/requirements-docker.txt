# Docker-optimized requirements with pinned versions for faster, reliable builds
# Use this for Docker builds: pip install -r requirements-docker.txt

# Core dependencies
fastapi==0.115.0
uvicorn==0.32.0
SQLAlchemy==2.0.36
alembic==1.14.0
pydantic[email]==2.10.0
python-multipart==0.0.12
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
pydantic-settings==2.6.0
slowapi==0.1.9
psycopg2-binary==2.9.10
fpdf2==2.8.1
APScheduler==3.10.4
joblib==1.4.2
email-validator==2.2.0

# Security and scanning
zaproxy==0.4.0
supabase==2.10.0
# Removed wapiti3 and mitmproxy due to complex dependency conflicts
# Use Docker sidecar containers for these tools instead
aiohttp-client-cache==0.12.0
PyJWT==2.10.0
cryptography==43.0.0

# ML and data processing (pinned stable versions)
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
# Removed catboost, torch, shap due to heavy dependencies - optional features
# These can be installed separately or used via API microservices
openai==1.55.0
groq==0.33.0

# Utilities and monitoring
redis==5.2.0
loguru==0.7.2
tqdm==4.67.0

# Web and networking
aiohttp==3.11.6
requests==2.32.3
httpx==0.27.2
websockets==14.0
fastapi-websocket-pubsub==0.3.9
dnspython==2.7.0

# Development and testing dependencies
pytest==8.3.3
pytest-asyncio==0.24.0
python-dotenv==1.0.1
